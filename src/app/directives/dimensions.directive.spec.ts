import { DimensionsDirective } from './dimensions.directive';

describe('DimensionsDirective', () => {
  it('should create an instance', () => {
    const directive = new DimensionsDirective();
    expect(directive).toBeTruthy();
  });
});
